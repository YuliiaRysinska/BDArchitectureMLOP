name: CI
on:
  workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Show workspace
        run: |
          echo ${{ github.workspace }}
          echo $GITHUB_WORKSPACE
